"use strict"; // treat all JS code as newer version

// alert(3 + 3) // we are using nodeJs, not brower. It will work in console in browser.

console.log(3 + 3)
// string
let name = "badal" // 'badal' also wrok
// number: Int and bigInt
let age = 22
// Boolean: true/false
let isLoggedIn = false
// null: standalone value
let temperature = null 
// undefined
let state // ; in end will also wrok
//symbol: to find the uniqueness component


//object

console.log(typeof age) // number
console.log(typeof "badal") // string
console.log(typeof temperature) // Object
console.log(typeof state)   // undefined

